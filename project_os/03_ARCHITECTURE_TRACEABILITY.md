# 03 — Architecture Traceability Matrix

### End-to-End Requirement Traceability

| Requirement Spec (Docs) | Backend API & Service | Frontend Component / Route | Database Table & Model | Runtime Verification | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **REQ-AUTH-01**: Registration & Argon2id Hashing | `IdentityService.register_user` (`POST /api/v1/auth/register`) | `apps/web/src/app/(auth)/register/page.tsx` | `users` (`User`), `workspaces` (`Workspace`) | User created, default workspace created, JWT issued | **COMPLETE** |
| **REQ-AUTH-02**: Authentication & Token Rotation | `IdentityService.authenticate_user` (`POST /api/v1/auth/login`, `POST /auth/refresh`) | `apps/web/src/app/(auth)/login/page.tsx`, `api-client.ts` | `sessions` (`Session`), `refresh_tokens` | Access JWT (15m) + Refresh JWT (7d) issued & auto-rotated | **COMPLETE** |
| **REQ-AUTH-03**: System Roles Query | `IdentityService.list_roles` (`GET /api/v1/auth/roles`) | `apps/web/src/hooks/use-workspace.ts` (`useRoles`) | `roles` (`Role`), `permissions` (`Permission`) | Roles returned to populate invitation role picker | **COMPLETE** |
| **REQ-WS-01**: Multi-Tenant Isolation | `get_current_workspace_id` FastAPI dependency | `api-client.ts` (`X-Workspace-ID` header interceptor) | Foreign Key `workspace_id` on all 17 tenant tables | 100% of outgoing requests attach workspace header | **COMPLETE** |
| **REQ-WS-02**: Tenant Context Switching | `WorkspaceService.get_user_workspaces` (`GET /api/v1/workspaces`) | `WorkspaceSwitcher.tsx`, `useWorkspaceStore` | `workspace_members` (`WorkspaceMember`) | Switching workspace purges React Query cache (`queryClient.invalidateQueries()`) | **COMPLETE** |
| **REQ-WS-03**: Workspace Settings & Metadata | `WorkspaceService.update_workspace` (`PATCH /api/v1/workspaces/{id}`) | `apps/web/src/app/(dashboard)/workspace/page.tsx` (Overview) | `workspaces` (`Workspace`) | Updates name, slug, industry, website, logo URL, size | **COMPLETE** |
| **REQ-WS-04**: Dynamic Regional Localization | `WorkspaceService.update_settings` (`GET/PATCH /workspaces/{id}/settings`) | `apps/web/src/hooks/use-formatters.ts`, `/workspace` (Settings) | `workspace_settings` (`WorkspaceSettings`) | Timezone, currency, date format, week start applied dynamically | **COMPLETE** |
| **REQ-WS-05**: Role-Based Member Invites | `WorkspaceService.invite_member` (`POST /workspaces/{id}/invitations`) | `apps/web/src/app/(dashboard)/workspace/page.tsx` (Members) | `workspace_invitations` (`WorkspaceInvitation`) | Single-use token generated with role binding | **COMPLETE** |
| **REQ-CRM-01**: Companies Management | `CRMService` (`GET`, `POST`, `GET /id`, `PATCH /id` `/companies`) | `apps/web/src/app/(dashboard)/companies/page.tsx`, `[id]/page.tsx` | `companies` (`Company`) | Full CRUD, filter, search, detail view, timeline widget | **COMPLETE** |
| **REQ-CRM-02**: Contacts Directory | `CRMService` (`GET`, `POST`, `GET /id`, `PATCH /id`, `DELETE /id` `/contacts`) | `apps/web/src/app/(dashboard)/contacts/page.tsx`, `[id]/page.tsx` | `contacts` (`Contact`) | Full CRUD, primary contact designation, company link | **COMPLETE** |
| **REQ-CRM-03**: Leads & Qualification | `CRMService` (`GET`, `POST`, `GET /id`, `PATCH /id`, `DELETE /id` `/leads`) | `apps/web/src/app/(dashboard)/leads/page.tsx` | `leads` (`Lead`) | Priority levels, est. value, search, disqualification | **COMPLETE** |
| **REQ-CRM-04**: Transactional Lead Conversion | `CRMService.convert_lead` (`POST /api/v1/leads/{id}/convert`) | `apps/web/src/app/(dashboard)/leads/page.tsx` (Convert Modal) | `companies`, `contacts`, `deals`, `leads` | Atomic creation of Company, Primary Contact, optional Deal | **COMPLETE** |
| **REQ-CRM-05**: Visual Sales Kanban Pipeline | `CRMService` (`GET /pipelines`, `GET /deals`, `POST /deals/{id}/move-stage`) | `apps/web/src/components/crm/kanban-board.tsx`, `/deals` | `pipelines`, `pipeline_stages`, `deals` | Drag-and-drop deal stage movement with probability | **COMPLETE** |
| **REQ-CRM-06**: Deal CRUD & Revenue Forecasting | `CRMService` (`POST`, `GET /id`, `PATCH /id`, `DELETE /id` `/deals`) | `apps/web/src/app/(dashboard)/deals/page.tsx`, `[id]/page.tsx` | `deals` (`Deal`) | Value tracking, close date, probability progress bar | **COMPLETE** |
| **REQ-CRM-07**: Tasks & Overdue Alerts | `CRMService` (`GET`, `POST`, `GET /id`, `PATCH /id`, `DELETE /id`, `POST /complete`) | `apps/web/src/app/(dashboard)/tasks/page.tsx` | `tasks` (`Task`) | Priority, due date datepicker, overdue alert warning (`⚠`) | **COMPLETE** |
| **REQ-CRM-08**: Immutable Activity Audit Trail | `CRMService._log_timeline_activity` (`GET /api/v1/timeline`) | `apps/web/src/components/crm/timeline-widget.tsx` | `activities` (`Activity`) | Immutable timeline feed with action icons & timestamps | **COMPLETE** |
| **REQ-BI-01**: Executive Dashboard Analytics | `AnalyticsService` (`GET /api/v1/analytics/overview`, `/leads`, `/deals`) | `apps/web/src/app/(dashboard)/dashboard/page.tsx` | Aggregated queries over `deals`, `leads`, `tasks` | Real-time KPI grid, pipeline breakdown, recent deals | **COMPLETE** |
| **REQ-SRCH-01**: Multi-Entity Global Search | `SearchService` (`GET /api/v1/search`) | `apps/web/src/components/common/global-search-bar.tsx` | PostgreSQL Full-Text Indexes | FTS results across companies, contacts, leads, deals | **COMPLETE** |
| **REQ-STOR-01**: MinIO Presigned File Storage | `StorageService` (`POST /api/v1/storage/upload-url`, `/confirm`) | `apps/web/src/hooks/use-storage.ts` | `attachments` (`Attachment`) | Presigned S3 upload URL issuance & attachment tracking | **OPERATIONAL** |
| **REQ-AI-01**: Lead Scoring & Summarization | `AIService` (`POST /api/v1/ai/score-lead`, `/summarize-deal`) | Backend rule engine + OpenAI client | Rule-based evaluation | Deterministic lead qualification & risk summary | **OPERATIONAL** |
